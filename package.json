{
  "name": "lensly",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "start": "node dist/index.js",
    "test:mock": "tsc && node dist/index.mock.js",
    "collect": "tsc && node dist/commands/collect.js",
    "evaluate": "tsc && node dist/commands/evaluate.js",
    "pipeline": "tsc && node dist/commands/pipeline.js",
    "status": "tsc && node dist/commands/status.js",
    "check:cerebras": "tsc && node dist/commands/check-cerebras.js",
    "check:groq": "tsc && node dist/commands/check-groq.js",
    "check:gemini": "tsc && node dist/commands/check-gemini.js",
    "queue": "tsc && node dist/commands/queue.js queue",
    "queue:status": "tsc && node dist/commands/queue.js status",
    "replenish": "tsc && node dist/commands/replenish.js",
    "publish:dry": "tsc && node dist/commands/publish-dry.js",
    "publish:due": "tsc && node dist/commands/publish-due.js",
    "publish:status": "tsc && node dist/commands/publish-status.js",
    "publish:create-test-due": "tsc && node dist/commands/publish-create-test-due.js",
    "publish:clear-test": "tsc && node dist/commands/publish-clear-test.js",
    "export:buffer": "tsc && node dist/commands/export-buffer.js",
    "export:buffer:status": "tsc && node dist/commands/export-buffer-status.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/luxon": "^3.7.2",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "date-fns-tz": "^3.2.0",
    "dotenv": "^16.6.1",
    "luxon": "^3.7.2",
    "rss-parser": "^3.13.0"
  }
}

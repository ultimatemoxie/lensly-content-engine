const https = require("https"); https.get("https://www.anthropic.com/news", {headers: {"User-Agent": "Mozilla/5.0"}}, (res) => { let data = ""; res.on("data", chunk => data += chunk); res.on("end", () => { const matches = data.match(/href="\/news\/[^"]+"/g) || []; console.log("Found", matches.length, "news links"); console.log(matches.slice(0, 10).join("\n")); }); }).on("error", (e) => console.error(e));
